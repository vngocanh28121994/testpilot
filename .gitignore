node_modules
dist/
build
artifacts/
reports/
runs/
testpilot.config.json
docs/
.DS_Store
*.log

# Test-account passwords. Never commit (see src/core/secrets.ts).
.testpilot.secrets.json

# Browser auth state is equivalent to a credential. Local fixture only.
.testpilot/sessions/

# Workflow history written by the UI.
registry/history.json

# Baseline snapshot for the parallel-devices refactor (local only).
.baseline-parallel/
registry/device-env.json

# Bản build của app UI mới (Phase 1+).
dist/ui/

# Kết quả chạy test của bảng điều khiển (Phase 3).
ui/coverage/
ui/playwright-report/
ui/test-results/
playwright-report/
test-results/
