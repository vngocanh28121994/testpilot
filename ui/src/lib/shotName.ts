// Compatibility export for callers outside the UI. The implementation lives
// with the standalone report so both renderers use one classification rule.
export { shotLabel, type ShotLabel } from '@evidence';
